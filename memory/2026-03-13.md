# 2026-03-13

## Lessons
- Brandon explicitly asked Wade to stop handing tasks back to him when tool access exists.
- New operating preference: when Brandon asks for help, Wade should execute directly across browser/tools/agents by default and only ask Brandon to act when a hard technical boundary prevents execution.
- Trigger context: repeated localhost auth popups in Chrome; Brandon wanted direct intervention rather than instructions.

## Actions taken
- Closed Chrome tab `http://localhost:19876/` (Daily Focus) via browser control.
- Identified local listener on port 19876 (`Python`, PID 77395) and force-killed it.
- Verified no remaining listener on port 19876 and confirmed localhost tab is no longer open.

## Additional durable context
- Root cause of recurring center-screen auth dialog was a macOS SecurityAgent prompt triggered by `security dump-keychain -d` / stash-related keychain access attempts; killing the triggering process and dismissing the dialog resolved it.
- Created new reusable skill: `skills/security-popup-killer/` with `SKILL.md` and `scripts/kill_security_popups.sh` (scan/fix/verify workflow).
- Confirmed Scratchpad project location and runtime: `~/.local/share/scratchpad/` served by `server.py` at `http://localhost:19876` (Daily Focus).
- Restarted scratchpad server and restored browser access to the real project page when Brandon requested the project (not static scratch file content).
- Implemented UI compaction pass for Scratchpad:
  - Prompts tab: denser category cards and compact prompt items for better information density.
  - Commands tab: smaller, cleaner command buttons with tighter spacing.
  - Today tab intentionally unchanged per Brandon request.

## Tasking + coordination updates
- Added requested items to Google Tasks (My Tasks list) and mirrored them to scratch paper, including:
  1) Wade doctrine vector DB + RAG/scraping agent
  2) vector DB connection feasibility for Claude Desktop/Claude Code
  3) naming Claude Code Desktop
  4) business social media setup
  5) define "stable enough" across life domains
  6) placeholder to clarify missing #6
  7) cron/task-agent for message queue/cue management and multi-agent coordination
- Scratch paper log location used: `~/command-center/SCRATCH-PAPER.md`.

## Agent collaboration preference
- Brandon explicitly requested Wade ask Claude Code directly for highest-impact support opportunities.
- Sent direct high-priority agent bus questions to `claude-code` and `desktop-claude` requesting the single highest-leverage action Wade can take for $50K MRR acceleration.

## Task language preference saved
- Brandon stated: when he says "tasks," he may mean ideas/projects/questions/tasks in natural language, and Wade should route the item to the appropriate bucket while still capturing it in Google Tasks.
- Added new idea to Google Tasks: "🟨 [IDEA] Productization agent: scrape MYTHOS outputs and auto-build digital products."
